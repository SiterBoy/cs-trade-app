export interface IEnvironmentVariables {
  APP_PORT: string;
  DB_HOST: string;
  DB_PORT: number;
  DB_NAME: string;
  DB_USERNAME: string;
  DB_PASSWORD: string;
  JWT_SECRET: string;
  ACCESS_TOKEN_EXPIRES_IN:string;
}